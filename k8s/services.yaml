---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: meridian
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: api-gateway
          image: meridian-api-gateway:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          env:
            - name: CHECKOUT_SERVICE_URL
              value: "http://checkout-service:8001"
            - name: INVENTORY_SERVICE_URL
              value: "http://inventory-service:8002"
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: meridian
spec:
  selector:
    app: api-gateway
  ports:
    - port: 8000
      targetPort: 8000
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout-service
  namespace: meridian
spec:
  replicas: 1
  selector:
    matchLabels:
      app: checkout-service
  template:
    metadata:
      labels:
        app: checkout-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8001"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: checkout-service
          image: meridian-checkout-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8001
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          env:
            # Healthy baseline: no injected faults. Incidents are meant to come
            # from real code changes or utilization/traffic shifts, not defaults.
            - name: ERROR_RATE
              value: "0"
            - name: SLOW_RATE
              value: "0"
            - name: PAYMENT_URL
              value: "http://payment-service:8004"
            - name: CHAOS_MODE
              valueFrom:
                configMapKeyRef:
                  name: meridian-config
                  key: chaos_mode
---
apiVersion: v1
kind: Service
metadata:
  name: checkout-service
  namespace: meridian
spec:
  selector:
    app: checkout-service
  ports:
    - port: 8001
      targetPort: 8001
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: meridian
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8004"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: payment-service
          image: meridian-payment-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8004
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          env:
            # Healthy baseline — faults are introduced via code changes, not defaults.
            - name: ERROR_RATE
              value: "0"
            - name: SLOW_RATE
              value: "0"
            - name: PROVIDER_DOWN
              value: "false"
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: meridian
spec:
  selector:
    app: payment-service
  ports:
    - port: 8004
      targetPort: 8004
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: meridian
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8002"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: inventory-service
          image: meridian-inventory-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8002
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          env:
            # Healthy baseline — faults are introduced via code changes, not defaults.
            - name: SLOW_QUERY_RATE
              value: "0"
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: meridian
spec:
  selector:
    app: inventory-service
  ports:
    - port: 8002
      targetPort: 8002
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: load-generator
  namespace: meridian
spec:
  replicas: 1
  selector:
    matchLabels:
      app: load-generator
  template:
    metadata:
      labels:
        app: load-generator
    spec:
      containers:
        - name: load-generator
          image: meridian-load-generator:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8003
          env:
            - name: GATEWAY_URL
              value: "http://api-gateway:8000"
            - name: RPS
              value: "5"
            - name: BURST_RPS
              value: "20"
---
apiVersion: v1
kind: Service
metadata:
  name: load-generator
  namespace: meridian
spec:
  selector:
    app: load-generator
  ports:
    - port: 8003
      targetPort: 8003
  type: LoadBalancer
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: meridian-config
  namespace: meridian
data:
  chaos_mode: "false"
