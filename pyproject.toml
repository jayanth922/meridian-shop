[tool.ruff]
target-version = "py312"
line-length = 140

[tool.ruff.lint]
# Explicit selection so lint behavior doesn't drift silently across ruff
# releases (ruff's own "default" rule set has changed between versions).
select = ["E", "F", "I", "UP", "PIE", "RUF"]
